{
  "buildCommand": "npm install --legacy-peer-deps && npm install ajv@^8 --legacy-peer-deps && npm run build",
  "outputDirectory": "build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
